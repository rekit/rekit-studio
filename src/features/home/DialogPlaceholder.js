import React, { Component } from 'react';

export default class DialogPlaceholder extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div className="home-dialog-placeholder">
        Component content: home/DialogPlaceholder
      </div>
    );
  }
}
